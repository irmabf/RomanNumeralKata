package app;

public class RomanNumeralsConverter {
    public String convert(int decimal) {
        if (decimal == 1)
            return "I";
        else if (decimal == 2)
            return "II";
      return "";
    }
}
